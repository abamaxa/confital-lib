//
//  rectangle.cpp
//  confital-lib-mactest
//
//  Created by Chris Morgan on 15/3/18.
//  Copyright © 2018 Chris Morgan. All rights reserved.
//

#include "rectangle.h"

/*
void Rectangle::calculate_intersections(const Line** lines) {
    lines[0]->find_intersection(*lines[2], m_bottom_left);
    lines[0]->find_intersection(*lines[3], m_bottom_right);
    lines[1]->find_intersection(*lines[3], m_top_right);
    lines[1]->find_intersection(*lines[2], m_top_left);
}

bool Rectangle::intersections_are_close(const PipelineJob& job) const {
    return (
        job.intersection_in_or_near_image(m_bottom_left) &&
        job.intersection_in_or_near_image(m_bottom_right) &&
        job.intersection_in_or_near_image(m_top_right) &&
        job.intersection_in_or_near_image(m_top_left)
    );
}
*/
